import type { RouteOptions } from "fastify";

/**
 * Represents a route definition that can be dynamically loaded and registered.
 * Extends Fastify's RouteOptions to ensure full compatibility.
 */
export type Route = RouteOptions;
